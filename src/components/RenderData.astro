---
import RenderUser from '@/components/RenderUser';

type Props = {
	id: number;
};

const { id } = Astro.props;

const user = await (await fetch(new URL(`/api/user/${id}`, Astro.url.origin))).json();
---

<RenderUser user={user} client:idle />
